storage:
  cls: postgresql
  db: postgresql:///?service=swh-storage-replica
  objstorage:
    cls: filtered
    storage_conf:
      cls: remote
      url: http:/nginx/rpc/objstorage/
    filters_conf:
      - type: readonly
