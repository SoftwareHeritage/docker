storage:
  cls: masking
  masking_db: postgresql:///?service=swh-mirror-masking
  max_pool_conns: 10
  storage:
    # Same as in conf/mirror/storage.yml
    cls: postgresql
    db: postgresql:///?dbname=swh-mirror-storage&host=swh-mirror-storage-db&user=postgres&password=testpassword
    objstorage:
      cls: filtered
      storage_conf:
        cls: remote
        url: http://nginx-mirror/rpc/objstorage/
      filters_conf:
        - type: readonly

masking_admin:
  masking_db: postgresql:///?service=swh-mirror-masking
